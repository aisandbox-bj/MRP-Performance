# Calibre MRP Performance

Supply-chain performance workbench (Calibre suite). Genesis commit — the v0.1.0-dev app lands in the next commit.

This is a public repository: code only. SAP extracts and datasets are never committed.
